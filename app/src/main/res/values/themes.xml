<resources xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Sticky" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <!-- RECT -->
    <style name="Button.Rect.Large.Primary" parent="Widget.AppCompat.Button">
        <item name="android:fontFamily">@font/spoqa_han_sans_regular</item>
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:background">@drawable/primary_rect_button</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <!-- PILL -->
    <style name="Button.Pill.Primary" parent="Widget.AppCompat.Button">
        <item name="android:fontFamily">@font/spoqa_han_sans_regular</item>
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:background">@drawable/primary_pill_button</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Button.Pill.Secondary" parent="Widget.AppCompat.Button">
        <item name="android:fontFamily">@font/spoqa_han_sans_regular</item>
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:background">@drawable/secondary_pill_button</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/accentPrimary</item>
    </style>

    <style name="Button.Pill.Large.Primary" parent="Button.Pill.Primary" />

    <style name="Button.Pill.Large.Secondary" parent="Button.Pill.Secondary" />

    <style name="Header.Icon.Leading.Tailing" parent="HeadlineXSmall">
        <item name="android:textColor">@color/black</item>
    </style>

</resources>